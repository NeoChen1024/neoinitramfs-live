exist()
{
	exist_bool=0
	for i in "/bin" "/sbin" "/usr/bin" "/usr/sbin" ; do
		if [ -x ${i}/"$1" ] ; then
			exist_bool=1
		fi
	done

	if [ exist_bool == 1 ] ; then
		return 0
	else
		return 1
	fi 
}

getsrc()
{
	if [ -v $1 ] ; then
		wget "$2"
	fi
}